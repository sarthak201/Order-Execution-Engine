version: '3.8'
services:
  redis:
    image: redis:7
    ports: ["6379:6379"]

  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: orders
    ports: ["5432:5432"]
